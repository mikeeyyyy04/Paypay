# Workspace Instructions

- Use React with TypeScript and Vite for the frontend.
- Keep the first slice focused on admin login and role protection.
- Prefer small, focused components and avoid unnecessary abstractions.
- Use local, frontend-only auth for the initial implementation unless a backend is introduced later.
- Keep admin UI screens reusable for classes, orders, and dashboard pages.
